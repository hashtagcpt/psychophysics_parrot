function [breakflag]=waitonclick(winID,fixsize,displace,screenrect)%function [breakflag]=waitonclick(winID,fixsize,displace,screenrect)%Waits for mouseclick on the fixation point. Retuns a break flag if the mouse is clicked in the %top left corner of the screen.%% Nov-23-98 : Created. (CPT)breakflag=0;dsplr=randperm(2); displace=[displace -displace];setmouse(screenrect(3)/2+displace(dsplr(1)),screenrect(4)/2,winID);waitticks(2); showcursor(301);fixrect=[screenrect(3)/2-fixsize*2 screenrect(4)/2-fixsize*2 screenrect(3)/2+fixsize*2 screenrect(4)/2+fixsize*2];x=screenrect(3); y=screenrect(2); button=0;while ~(inrect([x y], fixrect)) | (button < 1)	flushmouse;	[x y button]=getmouse(winID);	if (x == 0 & y==0) & (button > 0)		breakflag=1;		break;	endendreturn;